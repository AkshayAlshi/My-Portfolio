# Appium Mobile Tests (Skeleton)

This is a starter project. Update capabilities in `SmokePlaceholderTest.java` to point to your device and .apk/.ipa.

## Run (after updating capabilities)
```
mvn test
```
